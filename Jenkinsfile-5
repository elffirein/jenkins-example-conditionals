pipeline {	
   agent any
   stages {
	stage('Package the build output') {
		steps {
			script {
				def props = readJSON file: "${WORKSPACE}/config.json"
				version = props["version"]
				env.PACKAGE_VERSION=version
			}
			echo "The package version will be ${env.PACKAGE_VERSION}"
		}
	}
   }
}
